export enum TimerStateModel {
    IDLE = 'idle',
    INSPECTION = 'inspection',
    READY_WAITING = 'ready_waiting',
    READY_SET = 'ready_set',
    READY_GO = 'ready_go',
    RUNNING = 'running',
    STOPPED = 'stopped'
}